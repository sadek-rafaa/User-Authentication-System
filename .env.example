# Required for all environments
NODE_ENV=development
PORT=3000
JWT_SECRET=your_256bit_secure_secret_here
JWT_EXPIRES_IN=1d

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=auth_system

# Email (for development)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=